import { BackgroundHeroImg } from "../components";

const Courses = () => {
  return (
    <section className="school-courses">
      <h2>Courses Page</h2>
      <BackgroundHeroImg />
    </section>
  );
};

export default Courses;
